<!-- -slideshow.liquid-- -->
{% assign section_setting = section.settings %}
<div
  class="main-slider"
  data-dots="{{ section_setting.dots }}"
  data-arrows="{{ section_setting.buttons }}"
  data-infinite="{{ section_setting.infinite }}"
  data-autoplay="{{ section_setting.autoplay }}"
>
  <div class="slider">
    {% content_for 'blocks' %}
  </div>
</div>
<style>
  .slick-dots {
    bottom: 30px;
  }
  .main_slide {
    position: relative;
  }
  .content_wraper_slide {
    position: absolute;
    top: 50%;
    left: 20%;
    padding: 20px;
    background: #fff;
    border-radius: 5px;
    height: auto;
    width: 100%;
    max-width: 30%;
  }
</style>
{% schema %}
{
  "name": "t:sections.slideshow.main_slider",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.slideshow.slider_settings"
    },
    {
      "type": "checkbox",
      "id": "dots",
      "label": "t:sections.slideshow.dots_dots",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "buttons",
      "label": "t:sections.slideshow.buttons_buttons",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "infinite",
      "label": "t:sections.slideshow.infinite_infinite",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "t:sections.slideshow.autoplay_autoplay",
      "default": false
    }
  ],
  "class": "shopify_full_width",
  "presets": [
    {
      "name": "t:sections.slideshow.main_slider"
    }
  ],
  "blocks": [
    {
      "type": "slide"
    }
  ]
}
{% endschema %}
