{% if product.variants.size > 1 %}
  <div class="product-card__block product-card__block--variants" {{ block.shopify_attributes }}>
    <div class="product-card__variants">
      {% for variant in product.variants %}
        <button type="button" class="product-card__variant">{{ variant.title }}</button>
      {% endfor %}
    </div>
  </div>
{% endif %}

{% schema %}
{
  "name": "t:blocks.product-variants.product_variants",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_labels",
      "label": "t:blocks.product-variants.show_labels_show_variant_labels",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "t:blocks.product-variants.variants"
    }
  ]
}
{% endschema %}
